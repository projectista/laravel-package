<?php

use PHPUnit\Framework\TestCase;
use Projectista\LaravelPackage\Tests\FeatureTestCase;

uses(TestCase::class)->in('Arch');
uses(TestCase::class)->in('Unit');
uses(FeatureTestCase::class)->in('Feature');
